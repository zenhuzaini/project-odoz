<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%- include ../loadcss %>
</head>
<body>
    <table class="responsive-table">
        <thead>
          <tr>
              <th>What ?</th>
              <th>What's Inside</th>
              <th>Date Issue</th>
              <!-- <th>By</th> -->
              <th>ACtion</th>
              <th>Status</th>
          </tr>
        </thead>

        <tbody>
        <% for (var i=0; i < seeAllarticles.length; i++) { %>
          <tr>
            <td><%= seeAllarticles[i].article_title %></td>
            <td><%= seeAllarticles[i].article_date %></td>
            <td><%- seeAllarticles[i].article_description %></td>
            
            <td>
                <a href="viewarticleodoz/<%= seeAllarticles[i]._id %>" class="waves-effect waves-light btn-small"><i class="tiny material-icons">send</i>
                  Check
                </a>
                <a href="editarticleodoz/<%= seeAllarticles[i]._id %>" class="waves-effect waves-light btn-small"><i class="tiny material-icons">edit</i>
                  Edit
                </a>
                <a href="deletearticleodoz/<%= seeAllarticles[i]._id %>"
                  class="waves-effect waves-light btn-small">
                Delete
                </a>
            </td>
            <td><a
              class="<%= seeAllarticles[i].article_status ? 'waves-effect waves-light btn-small color: yellow' : 'waves-effect waves-light btn-small'%>"
              href="setstatusarticleodoz/<%= seeAllarticles[i]._id %>">
              <% if (seeAllarticles[i].article_status == false) { %>
                <i class="tiny material-icons">block</i> Not Active
                <% } else { %>
                 Active
                <% } %>
            </a>
            </td>
          </tr>
        <% } %> 
        </tbody>
      </table>
            
</body>
</html>